.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
# compute pay gaps in new columns
mutate(base_gap = round(((meanBase_Male - meanBase_Female) / meanBase_Male * 100), 2),
bonus_gap = round(((meanBonus_Male - meanBonus_Female) / meanBonus_Male * 100), 2)) |>
# remove "mean" from column names to standardise and prepare for lengthening
rename_with(\(x) gsub("mean", "", x) |> tolower()) |>
# lengthen for all columns
pivot_longer(everything()) |>
# separate first column into two
separate(col = name, into = c("pay_type", "x"), sep = "_") |>
# pivot wider
pivot_wider(names_from = x, values_from = value) |>
# change labeling for table
mutate(pay_type = ifelse(pay_type == "base", "Cơ bản", "Thưởng")) |>
# create html table
gt() |>
tab_header(title = "Trung bình Lương (USD) theo Giới tính và Chênh lệch (%)") |>
tab_footnote(footnote = "Chênh lệch = (Trung bình Nam - Trung bình Nữ) / Trung bình Nam * 100%") |>
tab_spanner(label = "Giới tính",
columns = c(female, male)) |>
cols_label(pay_type = "Loại lương",
female = "Nữ",
male = "Nam",
gap = "Chênh lệch") |>
cols_align(align = "center") |>
tab_style(style = "vertical-align:middle",
locations = cells_column_labels(columns = gap)) |>
tab_style(style = cell_text(size = "smaller"),
locations = cells_footnotes())
## Table of of overall mean pay and pay gap (UNFINISHED) ----
pay_clean |>
summarise(meanBase = mean(BasePay),
meanBonus = mean(Bonus),
.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
# compute pay gaps in new columns
mutate(base_gap = round(((meanBase_Male - meanBase_Female) / meanBase_Male * 100), 2),
bonus_gap = round(((meanBonus_Male - meanBonus_Female) / meanBonus_Male * 100), 2)) |>
# remove "mean" from column names to standardise and prepare for lengthening
rename_with(\(x) gsub("mean", "", x) |> tolower()) |>
# lengthen for all columns
pivot_longer(everything()) |>
# separate first column into two
separate(col = name, into = c("pay_type", "x"), sep = "_") |>
# pivot wider
pivot_wider(names_from = x, values_from = value) |>
# change labeling for table
mutate(pay_type = ifelse(pay_type == "base", "Cơ bản", "Thưởng")) |>
# create html table
gt() |>
tab_header(title = "Trung bình Lương (USD) theo Giới tính và Chênh lệch (%)") |>
tab_footnote(footnote = "Chênh lệch = (Trung bình Nam - Trung bình Nữ) / Trung bình Nam * 100%") |>
tab_spanner(label = "Giới tính",
columns = c(female, male)) |>
cols_label(pay_type = "Loại lương",
female = "Nữ",
male = "Nam",
gap = "Chênh lệch") |>
cols_align(align = "center") |>
tab_style(style = "vertical-align:middle",
locations = cells_column_labels(columns = pay_type, gap)) |>
tab_style(style = cell_text(size = "smaller"),
locations = cells_footnotes())
## Table of of overall mean pay and pay gap (UNFINISHED) ----
pay_clean |>
summarise(meanBase = mean(BasePay),
meanBonus = mean(Bonus),
.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
# compute pay gaps in new columns
mutate(base_gap = round(((meanBase_Male - meanBase_Female) / meanBase_Male * 100), 2),
bonus_gap = round(((meanBonus_Male - meanBonus_Female) / meanBonus_Male * 100), 2)) |>
# remove "mean" from column names to standardise and prepare for lengthening
rename_with(\(x) gsub("mean", "", x) |> tolower()) |>
# lengthen for all columns
pivot_longer(everything()) |>
# separate first column into two
separate(col = name, into = c("pay_type", "x"), sep = "_") |>
# pivot wider
pivot_wider(names_from = x, values_from = value) |>
# change labeling for table
mutate(pay_type = ifelse(pay_type == "base", "Cơ bản", "Thưởng")) |>
# create html table
gt() |>
tab_header(title = "Trung bình Lương (USD) theo Giới tính và Chênh lệch (%)") |>
tab_footnote(footnote = "Chênh lệch = (Trung bình Nam - Trung bình Nữ) / Trung bình Nam * 100%") |>
tab_spanner(label = "Giới tính",
columns = c(female, male)) |>
cols_label(pay_type = "Loại lương",
female = "Nữ",
male = "Nam",
gap = "Chênh lệch") |>
cols_align(align = "center") |>
tab_style(style = "vertical-align:middle",
locations = cells_column_labels(columns = c(pay_type, gap))) |>
tab_style(style = cell_text(size = "smaller"),
locations = cells_footnotes())
?percent
## Table of of overall mean pay and pay gap (UNFINISHED) ----
pay_clean |>
summarise(meanBase = mean(BasePay),
meanBonus = mean(Bonus),
.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
mutate(base_gap = percent(((meanBase_Male - meanBase_Female) / meanBase_Male), accuracy = 0.01),
bonus_gap = percent(((meanBonus_Male - meanBonus_Female) / meanBonus_Male), accuracy = 0.01))
## Table of of overall mean pay and pay gap (UNFINISHED) ----
pay_clean |>
summarise(meanBase = mean(BasePay),
meanBonus = mean(Bonus),
.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
mutate(base_gap = percent(((meanBase_Male - meanBase_Female) / meanBase_Male), accuracy = 0.01),
bonus_gap = percent(((meanBonus_Male - meanBonus_Female) / meanBonus_Male), accuracy = 0.01))
## Table of of overall mean pay and pay gap (UNFINISHED) ----
pay_clean |>
summarise(meanBase = mean(BasePay),
meanBonus = mean(Bonus),
.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
# compute pay gaps in new columns
mutate(base_gap = percent(((meanBase_Male - meanBase_Female) / meanBase_Male), accuracy = 0.01),
bonus_gap = percent(((meanBonus_Male - meanBonus_Female) / meanBonus_Male), accuracy = 0.01)) |>
# remove "mean" from column names to standardise and prepare for lengthening
rename_with(\(x) gsub("mean", "", x) |> tolower()) |>
# lengthen for all columns
pivot_longer(everything()) |>
# separate first column into two
separate(col = name, into = c("pay_type", "x"), sep = "_") |>
# pivot wider
pivot_wider(names_from = x, values_from = value) |>
# change labeling for table
mutate(pay_type = ifelse(pay_type == "base", "Cơ bản", "Thưởng")) |>
# create html table
gt() |>
tab_header(title = "Trung bình Lương (USD) theo Giới tính và Chênh lệch (%)") |>
tab_footnote(footnote = "Chênh lệch = (Trung bình Nam - Trung bình Nữ) / Trung bình Nam * 100%") |>
tab_spanner(label = "Giới tính",
columns = c(female, male)) |>
cols_label(pay_type = "Loại lương",
female = "Nữ",
male = "Nam",
gap = "Chênh lệch") |>
cols_align(align = "center") |>
tab_style(style = "vertical-align:middle",
locations = cells_column_labels(columns = c(pay_type, gap))) |>
tab_style(style = cell_text(size = "smaller"),
locations = cells_footnotes())
## Table of of overall mean pay and pay gap (UNFINISHED) ----
pay_clean |>
summarise(meanBase = mean(BasePay),
meanBonus = mean(Bonus),
.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
# compute pay gaps in new columns
mutate(base_gap = (meanBase_Male - meanBase_Female) / meanBase_Male,
bonus_gap = (meanBonus_Male - meanBonus_Female) / meanBonus_Male) |>
# remove "mean" from column names to standardise and prepare for lengthening
rename_with(\(x) gsub("mean", "", x) |> tolower()) |>
# lengthen for all columns
pivot_longer(everything()) |>
# separate first column into two
separate(col = name, into = c("pay_type", "x"), sep = "_") |>
# pivot wider
pivot_wider(names_from = x, values_from = value) |>
# change labeling for table
mutate(pay_type = ifelse(pay_type == "base", "Cơ bản", "Thưởng")) |>
# create html table
gt() |>
tab_header(title = "Trung bình Lương (USD) theo Giới tính và Chênh lệch (%)") |>
tab_footnote(footnote = "Chênh lệch = (Trung bình Nam - Trung bình Nữ) / Trung bình Nam * 100%") |>
tab_spanner(label = "Giới tính",
columns = c(female, male)) |>
cols_label(pay_type = "Loại lương",
female = "Nữ",
male = "Nam",
gap = "Chênh lệch") |>
cols_align(align = "center") |>
tab_style(style = "vertical-align:middle",
locations = cells_column_labels(columns = c(pay_type, gap))) |>
tab_style(style = cell_text(size = "smaller"),
locations = cells_footnotes())
pay_clean |>
summarise(meanBase = mean(BasePay),
meanBonus = mean(Bonus),
.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
# compute pay gaps in new columns
mutate(base_gap = (meanBase_Male - meanBase_Female) / meanBase_Male,
bonus_gap = (meanBonus_Male - meanBonus_Female) / meanBonus_Male) |>
# remove "mean" from column names to standardise and prepare for lengthening
rename_with(\(x) gsub("mean", "", x) |> tolower()) |>
# lengthen for all columns
pivot_longer(everything()) |>
# separate first column into two
separate(col = name, into = c("pay_type", "x"), sep = "_") |>
# pivot wider
pivot_wider(names_from = x, values_from = value)
## Table of of overall mean pay and pay gap (UNFINISHED) ----
pay_clean |>
summarise(meanBase = mean(BasePay),
meanBonus = mean(Bonus),
.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
# compute pay gaps in new columns
mutate(base_gap = (meanBase_Male - meanBase_Female) / meanBase_Male,
bonus_gap = (meanBonus_Male - meanBonus_Female) / meanBonus_Male) |>
# remove "mean" from column names to standardise and prepare for lengthening
rename_with(\(x) gsub("mean", "", x) |> tolower()) |>
# lengthen for all columns
pivot_longer(everything()) |>
# separate first column into two
separate(col = name, into = c("pay_type", "x"), sep = "_") |>
# pivot wider
pivot_wider(names_from = x, values_from = value) |>
# change pay_type levels for table and convert gap to %
mutate(pay_type = ifelse(pay_type == "base", "Cơ bản", "Thưởng"),
gap = percent(gap, accuracy = 0.01)) |>
# create html table
gt() |>
tab_header(title = "Trung bình Lương (USD) theo Giới tính và Chênh lệch (%)") |>
tab_footnote(footnote = "Chênh lệch = (Trung bình Nam - Trung bình Nữ) / Trung bình Nam * 100%") |>
tab_spanner(label = "Giới tính",
columns = c(female, male)) |>
cols_label(pay_type = "Loại lương",
female = "Nữ",
male = "Nam",
gap = "Chênh lệch") |>
cols_align(align = "center") |>
tab_style(style = "vertical-align:middle",
locations = cells_column_labels(columns = c(pay_type, gap))) |>
tab_style(style = cell_text(size = "smaller"),
locations = cells_footnotes())
pay_clean |>
summarise(meanBase = mean(BasePay),
meanBonus = mean(Bonus),
.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
# compute pay gaps in new columns
mutate(base_gap = (meanBase_Male - meanBase_Female) / meanBase_Male,
bonus_gap = (meanBonus_Male - meanBonus_Female) / meanBonus_Male) |>
# remove "mean" from column names to standardise and prepare for lengthening
rename_with(\(x) gsub("mean", "", x) |> tolower()) |>
# lengthen for all columns
pivot_longer(everything()) |>
# separate first column into two
separate(col = name, into = c("pay_type", "x"), sep = "_") |>
# pivot wider
pivot_wider(names_from = x, values_from = value) |>
# change pay_type levels for table, convert gap to %, round values to 2 decimal places
mutate(pay_type = ifelse(pay_type == "base", "Cơ bản", "Thưởng"),
gap = percent(gap, accuracy = 0.01),
female = round(female, 2),
male = round(male, 2))
## Table of of overall mean pay and pay gap (UNFINISHED) ----
pay_clean |>
summarise(meanBase = mean(BasePay),
meanBonus = mean(Bonus),
.by = Gender) |> # no ungrouping needed
# pivot wider to compute pay gaps from the columns
pivot_wider(names_from = Gender,
values_from = c(meanBase, meanBonus)) |>
# compute pay gaps in new columns
mutate(base_gap = (meanBase_Male - meanBase_Female) / meanBase_Male,
bonus_gap = (meanBonus_Male - meanBonus_Female) / meanBonus_Male) |>
# remove "mean" from column names to standardise and prepare for lengthening
rename_with(\(x) gsub("mean", "", x) |> tolower()) |>
# lengthen for all columns
pivot_longer(everything()) |>
# separate first column into two
separate(col = name, into = c("pay_type", "x"), sep = "_") |>
# pivot wider
pivot_wider(names_from = x, values_from = value) |>
# change pay_type levels for table, convert gap to %, round values to 2 decimal places
mutate(pay_type = ifelse(pay_type == "base", "Cơ bản", "Thưởng"),
gap = percent(gap, accuracy = 0.01),
female = round(female, 2),
male = round(male, 2)) |>
# create html table
gt() |>
tab_header(title = "Trung bình Lương (USD) theo Giới tính và Chênh lệch (%)") |>
tab_footnote(footnote = "Chênh lệch = (Trung bình Nam - Trung bình Nữ) / Trung bình Nam * 100%") |>
tab_spanner(label = "Giới tính",
columns = c(female, male)) |>
cols_label(pay_type = "Loại lương",
female = "Nữ",
male = "Nam",
gap = "Chênh lệch") |>
cols_align(align = "center") |>
tab_style(style = "vertical-align:middle",
locations = cells_column_labels(columns = c(pay_type, gap))) |>
tab_style(style = cell_text(size = "smaller"),
locations = cells_footnotes())
pay_clean |>
summarise(mean = mean(BasePay),
.by = c(AgeGroup, Gender)) |>
pivot_wider(names_from = Gender,
values_from = mean) |>
# Compute pay gap
mutate(base_gap = (Male - Female) / Male) |>
# meanbonus_age <- pay_clean |>
#   summarise(mean = mean(Bonus),
#             .by = c(AgeGroup, Gender)) |>
#   pivot_wider(names_from = Gender,
#               values_from = mean) |>
#   mutate(bonus_gap = (Male - Female) / Male)
# meantotal_age <- pay_clean |>
#   summarise(mean = mean(TotalPay),
#             .by = c(AgeGroup, Gender)) |>
#   pivot_wider(names_from = Gender,
#               values_from = mean) |>
#   mutate(total_gap = (Male - Female) / Male)
# Create bar plots
ggplot(aes(x = AgeGroup, y = base_gap, label = percent(base_gap))) +
geom_col() +
geom_text(nudge_y = 0.005) +
scale_y_continuous(breaks = seq(0, 1, by = 0.05),
limits = c(0, 0.2),
expand = c(0, 0),
labels = label_percent(),
name = "Chênh lệch (%)") +
labs(title = "Chênh lệch về Lương cơ bản giữa Nam và Nữ theo Nhóm tuổi",
subtitle = "Chênh lệch = (Lương TB của Nam - Lương TB của Nữ) / Lương TB của Nam * 100%") +
theme(
panel.border = element_rect(colour = "black"),
panel.background = element_blank()
)
??gtsummary
# Load required packages through package manager "pacman" ----
if (!require("pacman")) install.packages("pacman")
pacman::p_load(tidyverse, # for any additional wrangling needed
Hmisc, # for correlation with p-values
psych, # for data summary
multcomp, reghelper, # for linear combinations
rlang, interactions, # for any regions of significance
prediction, # for predicted outcomes
gtsummary, # for linear regression tables
here) # for relative file paths
# Load functions for customised outputs from Prof Lesa Hoffman ----
load("DrHoffman_Functions.R")
# Load functions for customised outputs from Prof Lesa Hoffman ----
load(here::here("Rscripts", "DrHoffman_Functions.R"))
# Load functions for customised outputs from Prof Lesa Hoffman ----
source(here::here("Rscripts", "DrHoffman_Functions.R"))
# Import cleaned data set
load(here:here("data", "pay_clean.RData"))
# Import cleaned data set
load(here:here("data", "pay_clean.RData"))
load(here:here("data", "pay_clean.RData"))
load("~/data science/gender-pay-gap/data/pay_clean.RData")
# Import cleaned data set
load(here::here("data", "pay_clean.RData"))
?t.test
# T-test to compare mean base pay between Male and Female ----
t.test(BasePay ~ Gender, data = pay_clean)
Model1 <- lm(BasePay ~ Gender, data = pay_clean)
LMsummary(Model1)
LMsummary(Model1)
obj <- LMsummary(Model1)
# T-test to compare mean base pay between Male and Female ----
t.test(BasePay ~ Gender, data = pay_clean)
View(pay_clean)
# GLM with all variables
ModelFull <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age + PerfEval,
data = pay_clean)
obj <- LMsummary(ModelFull)
Model3 <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age,
data = pay_clean)
obj <- LMsummary(Model3)
ModelFull <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age + PerfEval + Bonus,
data = pay_clean)
obj <- LMsummary(ModelFull)
## Gender ----
ModelNoGender <- lm(BasePay ~ Education + Dept + Seniority + Age + PerfEval + Bonus,
data = pay_clean)
R2compare(ModelNoGender, ModelFull, Gender)
R2compare(ModelNoGender, ModelFull, "Gender")
R2compare(ReducedModel = ModelNoGender, FullModel = ModelFull, PredName = "Gender")
R2compare(ReducedModel = ModelNoGender, FullModel = ModelFull, PredName = "Gender",
digits = 3, explain = T)
R2compare(ReducedModel = ModelNoGender, FullModel = ModelFull, PredName = "Gender")
R2compare(ReducedModel = ModelNoGender, FullModel = ModelFull,
PredName = "Gender", explain = T)
ModelNoEdu <- lm(BasePay ~ Gender + Dept + Seniority + Age + PerfEval + Bonus,
data = pay_clean)
R2compare(ReducedModel = ModelNoEdu, FullModel = ModelFull,
PredName = "Education", explain = T)
ModelNoDept <- lm(BasePay ~ Gender + Education + Seniority + Age + PerfEval + Bonus,
data = pay_clean)
R2compare(ReducedModel = ModelNoDept, FullModel = ModelFull,
PredName = "Dept", explain = T)
ModelNoSenior <- lm(BasePay ~ Gender + Education + Dept + Age + PerfEval + Bonus,
data = pay_clean)
R2compare(ReducedModel = ModelNoSenior, FullModel = ModelFull,
PredName = "Seniority", explain = T)
ModelNoAge <- lm(BasePay ~ Gender + Education + Dept + Seniority + PerfEval + Bonus,
data = pay_clean)
R2compare(ReducedModel = ModelNoAge, FullModel = ModelFull,
PredName = "Age", explain = T)
ModelNoEval <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age + Bonus,
data = pay_clean)
R2compare(ReducedModel = ModelNoEval, FullModel = ModelFull,
PredName = "PerfEval", explain = T)
ModelNoBonus <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age + PerfEval,
data = pay_clean)
R2compare(ReducedModel = ModelNoBonus, FullModel = ModelFull,
PredName = "Bonus", explain = T)
Model2 <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age,
data = pay_clean)
obj <- LMsummary(Model2)
obj <- LMsummary(Model2, effectsizes = T)
Model2NoGender <- lm(BasePay ~ Education + Dept + Seniority + Age,
data = pay_clean)
R2compare(ReducedModel = Model2NoGender, FullModel = Model2,
PredName = "Gender", explain = T)
Model2NoEdu <- lm(BasePay ~ Gender + Dept + Seniority + Age,
data = pay_clean)
R2compare(ReducedModel = Model2NoEdu, FullModel = Model2,
PredName = "Education", explain = T)
Model2NoDept <- lm(BasePay ~ Gender + Education + Seniority + Age,
data = pay_clean)
R2compare(ReducedModel = Model2NoDept, FullModel = Model2,
PredName = "Dept", explain = T)
ModelNoDept <- lm(BasePay ~ Gender + Education + Seniority + Age + PerfEval + Bonus,
data = pay_clean)
R2compare(ReducedModel = ModelNoDept, FullModel = ModelFull,
PredName = "Dept", explain = T)
Model2NoSenior <- lm(BasePay ~ Gender + Education + Dept + Age,
data = pay_clean)
R2compare(ReducedModel = Model2NoSenior, FullModel = Model2,
PredName = "Seniority", explain = T)
Model2NoAge <- lm(BasePay ~ Gender + Education + Dept + Seniority,
data = pay_clean)
R2compare(ReducedModel = Model2NoAge, FullModel = Model2,
PredName = "Age", explain = T)
pacman::p_load(tidyverse, # for any additional wrangling needed
Hmisc, # for correlation with p-values
psych, # for data summary
multcomp, reghelper, # for linear combinations
rlang, interactions, # for any regions of significance
prediction, # for predicted outcomes
gtsummary, # for linear regression tables
here, # for relative file paths
table1) # for descriptive stats table
?table1
# Examining any interaction effects ----
table1::table1(~ Education + Dept + Seniority + Age | Gender,
data = pay_clean)
## Frequency table ----
table1::table1(~ Gender | Education,
data = pay_clean)
## Frequency table ----
table1::table1(~ Gender | Education + Dept,
data = pay_clean)
## Frequency table ----
table1::table1(~ Gender | Education,
data = pay_clean)
table1::table1(~ Gender | Dept,
data = pay_clean)
## Frequency table ----
table1::table1(~ Gender | Education, overall = F,
data = pay_clean)
table1::table1(~ Gender | Dept, overall = F,
data = pay_clean)
table1::table1(~ Gender | Seniority, overall = F,
data = pay_clean)
table1::table1(~ Gender | Age, overall = F,
data = pay_clean)
?boxplot
boxplot(Age ~ Gender, data = pay_clean)
Model3 <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age + Gender:Seniority,
data = pay_clean)
obj <- LMsummary(Model3, effectsizes = T)
Model4 <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age + Gender:Age,
data = pay_clean)
obj <- LMsummary(Model4, effectsizes = T) # nope
Model5 <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age + Gender:Education,
data = pay_clean)
obj <- LMsummary(Model5, effectsizes = T) # nope
Model6 <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age + Gender:Dept,
data = pay_clean)
obj <- LMsummary(Model6, effectsizes = T) # nope
Model2 <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age,
data = pay_clean)
obj <- LMsummary(Model2, effectsizes = T)
Model7 <- lm(BasePay ~ Gender + Education + Dept + Seniority + Age + Age:Seniority,
data = pay_clean)
obj <- LMsummary(Model7, effectsizes = T) # nope
# Summary table for (tentative) final model ----
gtsummary::tbl_regression(Model2)
obj <- LMsummary(Model2, effectsizes = T)
